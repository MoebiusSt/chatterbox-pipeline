parent: defaults/chatterbox_std.yaml

job:
  name: simple-example
  run_label: 
input:
  text_file: input-document.txt
generation:
  num_candidates: 2
  max_retries: 1
  default_speaker: stephan_moebius_2

# All other settings are inherited from defaults/chatterbox_std.yaml → default_config.yaml